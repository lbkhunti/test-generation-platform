const express = require('express');\nconst { getTestAnalytics, getQuestionAnalytics, getStudentAnalytics } = require('../controllers/analyticsController');\nconst { verifyToken } = require('../middleware/authMiddleware');\n\nconst router = express.Router();\n\nrouter.get('/test/:testId', verifyToken, getTestAnalytics);\nrouter.get('/question/:questionId', verifyToken, getQuestionAnalytics);\nrouter.get('/student/:studentId', verifyToken, getStudentAnalytics);\n\nmodule.exports = router;
